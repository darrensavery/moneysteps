/**
 * AboutSettings — re-exports SupportSettings under the original name so the
 * ParentSettingsTab import path stays unchanged.
 */

export { SupportSettings as AboutSettings } from './SupportSettings'
